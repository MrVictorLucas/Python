# Criando uma Def
def soma(x, y):
    return x + y

# Utilizando a def
x = int(input("Digite um número: "))
y = int(input("Digite um outro número: "))
soma = soma(x, y)

print(f"A soma entre {x} + {y} = {soma}")